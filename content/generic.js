// content/generic.js — last-resort copy, used only if the hero's own
// required content (content/hero.js, content/pricing.js) is somehow
// missing or fails canRender(). Deliberately minimal and safe.
const CONTENT_GENERIC = {
  generic: {
    headline: "Full home interiors, done right.",
    sub: "Fixed pricing in writing. On-time delivery. A written warranty."
  }
};
